import React from 'react';
import {FormInterface} from "../types/type";

const FormContainer : React.FC<FormInterface> = ({handleFormSubmit, setTask} :FormInterface ) => {
    return (
        <form

        >

        </form>
    );
};

export default FormContainer;
